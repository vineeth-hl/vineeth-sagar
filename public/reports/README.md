# Project reports

Drop the PDF reports referenced by blog post frontmatter here, e.g.:

- rag-pipeline-technical-report.pdf
- tgnn-fraud-detection-report.pdf

The `reportUrl` field in each `.mdx` maps to `/reports/<file>.pdf`.
