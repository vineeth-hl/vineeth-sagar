# Project reports

Drop the PDF reports referenced by blog-post frontmatter here. Current posts expect:

- `rag-pipeline-technical-report.pdf`  — Building a Production RAG Pipeline
- `zero-day-detection-report.pdf`      — Zero-Day: Kubernetes Attack Detection & Mitigation

The `reportUrl` field in each `.mdx` maps to `/reports/<file>`.
